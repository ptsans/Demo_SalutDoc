{
  "name": "server",
  "version": "0.1.0",
  "description": "Demo server for SalutDoc",
  "private": true,
  "scripts": {
    "node_dev": "ts-node-dev --project ./tsconfig.json --transpile-only -r tsconfig-paths/register src/**/*.ts ./src/app/main.ts"
  },
  "dependencies": {
    "fastify": "^3.14.0",
    "sqlite3": "^5.0.2",
    "fastify-cors": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "@types/sqlite3": "^3.1.7",
    "ts-node-dev": "^1.1.6",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.2.3"
  }
}
